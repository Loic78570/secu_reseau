executing program : `sudo venv/bin/python3 (program).py`<br>
creating a venv : `virtualenv venv`<br>
launching venv : `./venv/bin/activate`<br>
<p>Avoid launching program outside venv. Can break python (on linux too)</p>